import styled from 'styled-components';

export const SectionListWrapper = styled.ul`
  margin: 0;
  padding: 0;
  list-style-type: none;
  overflow-x: hidden;
`;

export const ImgWrapper = styled.div`
  width: 24px;
  margin-right: 12px;
`;
