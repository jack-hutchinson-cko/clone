<IBuilderDescriptionCard title="Create the form" tab="payment-form.html" lines={[40, 81]}>

Frames handles the payment information through multiple iframes, so your website doesn’t touch sensitive information. We insert an iframe between each placeholder `div`. 

- Card number: `<div class="card-number-frame"></div>`
- Expiry date: `<div class="expiry-date-frame"></div>`
- CVV frame: `<div class="cvv-frame"></div>`

<TipBox variant="tip">

Remember to change the value for the action attribute to the URL the token needs to be sent to.

</TipBox>

</IBuilderDescriptionCard>
