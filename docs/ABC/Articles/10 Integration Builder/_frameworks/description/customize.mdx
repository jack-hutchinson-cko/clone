<IBuilderDescriptionCard lines={[7, 15]} tab="app.js" >

You can customize the language, CSS and placeholders on input fields in the iframes. Do this when initializing Frames by specifying options as another argument. For example:

```js
Frames.init(
‘pk_test_xxx’, {
{
	localization: "FR-FR",
	style: {
		base: {
			color: "black",
			fontSize: "18px",
		},
	},
)
```

Available CSS options:

<Table>
<TableBody>
<TableRow>
<TableCell>

`base`

</TableCell>
<TableCell>

The default style.

</TableCell>
</TableRow>
<TableRow>
<TableCell>

`focus`

</TableCell>
<TableCell>

Rules applied when the element is focused.

</TableCell>
</TableRow>
<TableRow>
<TableCell>

`hover`

</TableCell>
<TableCell>

Rules applied when the element is hovered over.

</TableCell>
</TableRow>
<TableRow>
<TableCell>

`invalid`

</TableCell>
<TableCell>

Rules applied when the element is invalid.

</TableCell>
</TableRow>
<TableRow>
<TableCell>

`placeholder`

</TableCell>
<TableCell>

Accepts an object with base and focus.

</TableCell>
</TableRow>
<TableRow>
<TableCell>

`valid`

</TableCell>
<TableCell>

Rules applied when the element is valid.

</TableCell>
</TableRow>
</TableBody>
</Table>

See our [Frames customization guide](/integrate/frames/frames-customization-guide) for more examples.

</IBuilderDescriptionCard>

<IBuilderDescriptionCard title="Choose a theme" lines={[11, 11]} tab="app.js" >

Pick a color scheme that suits your style or define your own specific colour.

<IBuilderCodeControl
  controlType="select"
  optionType="frameColorSelector"
  options={[
    { option: '#000000', codeVariables: ['#000000'] },
    { option: '#EE6D5F', codeVariables: ['#EE6D5F'] },
    { option: '#009E7E', codeVariables: ['#009E7E'] },
    { option: '#F3C25D', codeVariables: ['#F3C25D'] },
    { option: '#3AB9F4', codeVariables: ['#3AB9F4'] },
    { option: '#6964B3', codeVariables: ['#6964B3'] }
  ]}
>

```
      /* Inserted color data */
      color: $1,
```

</IBuilderCodeControl>

</IBuilderDescriptionCard>

<IBuilderDescriptionCard title="Choose a font size" lines={[12, 12]} tab="app.js" >

Choose the size of the text displayed in your Frame.

<IBuilderCodeControl
  controlType="select"
  optionType="frameFontSizeSelector"
  options={[
    { option: '12', codeVariables: ['12'] },
    { option: '14', codeVariables: ['14'] },
    { option: '16', codeVariables: ['16'] }
  ]}
>

```
      /* Inserted fontSize data */
      fontSize: $1px,
```

</IBuilderCodeControl>

</IBuilderDescriptionCard>

<IBuilderDescriptionCard title="Select a language" lines={[8, 8]} tab="app.js" >

Choose the size of the text displayed in your Frame.

<IBuilderCodeControl
  controlType="select"
  optionType="frameLanguageSelector"
  options={[
    { option: 'EN-GB', codeVariables: ['EN-GB'] },
    { option: 'NL-NL', codeVariables: ['NL-NL'] },
    { option: 'FR-FR', codeVariables: ['FR-FR'] },
    { option: 'DE-DE', codeVariables: ['DE-DE'] },
    { option: 'IT-IT', codeVariables: ['IT-IT'] },
    { option: 'KO-KR', codeVariables: ['KO-KR'] },
    { option: 'ES-ES', codeVariables: ['ES-ES'] }
  ]}
>

```
  /* Inserted localization */
  localization: "$1",
```

</IBuilderCodeControl>

</IBuilderDescriptionCard>